% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netutil.R
\name{fed_friendly_http_error}
\alias{fed_friendly_http_error}
\title{Translate a low-level HTTP/connection failure into a friendly message}
\usage{
fed_friendly_http_error(e = NULL, status_code = NULL)
}
\arguments{
\item{e}{Condition object from a failed \code{httr::POST()}/\code{GET()}
call (connection-level failure), or \code{NULL} if the call completed
but returned a non-2xx status.}

\item{status_code}{HTTP status code, or \code{NULL} for a connection-level
failure.}
}
\value{
A friendly message string, or \code{NULL} if this isn't one of
  the recognised cases.
}
\description{
Only the three specific failure modes named in the Phase 3 charter are
translated; anything else returns \code{NULL} so the caller keeps its
existing (more specific) message — e.g. a 400 from a \code{min_n}
validation failure already carries a useful, specific message that must
not be replaced by a generic one.
}
